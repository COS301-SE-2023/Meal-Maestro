import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'meal-maestro',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
